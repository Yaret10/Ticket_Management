name: Java and SQL Server verification
on: [push, pull_request]
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: maven
      - run: chmod +x mvnw
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - run: node --test scripts/frontend-tests.mjs
      - run: ./mvnw -B fmt:check verify
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: |
            target/surefire-reports
            target/failsafe-reports
